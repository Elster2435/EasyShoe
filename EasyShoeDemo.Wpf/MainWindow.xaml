<Window x:Class="EasyShoeDemo.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasyShoeDemo.Wpf"
        mc:Ignorable="d"
        Title="Товары"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="Times New Roman">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem x:Name="ProductsMenuItems" Header="Товары">
                <MenuItem x:Name="AddProduct" Header="Добавить товар" Click="AddProduct_Click"/>
                <MenuItem x:Name="UpdateProduct" Header="Редактировать товар" Click="UpdateProduct_Click"/>
                <MenuItem x:Name="DeleteProduct" Header="Удалить товар" Click="DeleteProduct_Click"/>
            </MenuItem>
            <MenuItem x:Name="Exit" Header="Выход" Click="Exit_Click"/>
        </Menu>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Список товаров" Grid.Row="0" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold"/>
            <TextBlock x:Name="UserInfoTextBlock" Grid.Row="1" HorizontalAlignment="Left" FontSize="16" FontWeight="Bold" Margin="10"/>
            <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="10">
                <TextBox x:Name="SearchTextBox" Width="125" Margin="0,0,15,0" TextChanged="SearchTextBox_TextChanged"/>
                <ComboBox x:Name="CategoryComboBox" Width="125" Margin="0,0,15,0" DisplayMemberPath="Name" SelectedValuePath="Id" SelectionChanged="CategoryComboBox_SelectionChanged"/>
                <ComboBox x:Name="ManufacturerComboBox" Width="125" Margin="0,0,15,0" DisplayMemberPath="Name" SelectedValuePath="Id" SelectionChanged="ManufacturerComboBox_SelectionChanged"/>
                <ComboBox x:Name="SupplierComboBox" Width="125" Margin="0,0,15,0" DisplayMemberPath="Name" SelectedValuePath="Id" SelectionChanged="SupplierComboBox_SelectionChanged"/>
                <ComboBox x:Name="SortComboBox" Width="125" SelectedIndex="0" SelectionChanged="SortComboBox_SelectionChanged">
                    <ComboBoxItem Content="Без сортировки" Tag=""/>
                    <ComboBoxItem Content="По возрастанию" Tag="asc"/>
                    <ComboBoxItem Content="По убыванию" Tag="desc"/>
                </ComboBox>
            </StackPanel>
            <ListView x:Name="ProductsListView" BorderThickness="1" Background="White" Grid.Row="3">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,10" Padding="10" BorderBrush="Black" BorderThickness="1" Width="900">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="White"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsOutOfStock}" Value="True">
                                            <Setter Property="Background" Value="LightBlue"/>
                                        </DataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsOutOfStock}" Value="False"/>
                                                <Condition Binding="{Binding HasBigDiscount}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Background" Value="#2E8B57"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="170"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="150" Height="120" BorderBrush="Gray" BorderThickness="1">
                                    <Image Source="{Binding DisplayPhotoPath}" Stretch="Uniform"/>
                                </Border>
                                <StackPanel Grid.Column="1" Margin="10,0">
                                    <TextBlock FontWeight="Bold" FontSize="16">
                                        <Run Text="{Binding Category.Name}"/>
                                        <Run Text=" | "/>
                                        <Run Text="{Binding Name}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Description, StringFormat=Описание: {0}}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                    <TextBlock Text="{Binding Manufacturer.Name, StringFormat=Производитель: {0}}"/>
                                    <TextBlock Text="{Binding Supplier.Name, StringFormat=Поставщик: {0}}"/>
                                    <TextBlock Text="{Binding Unit, StringFormat=Единица измерения: {0}}"/>
                                    <TextBlock Text="{Binding StockQuantity, StringFormat=Количество на складе: {0}}"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <TextBlock Text="Цена: "/>
                                        <TextBlock Text="{Binding Price}" Foreground="Red" TextDecorations="Strikethrough">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasDiscount}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="{Binding DiscountedPrice}" Margin="5,0,0,0"/>
                                        <TextBlock Text="{Binding Price}" Margin="5,0,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasDiscount}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>
                                <TextBlock Grid.Column="2"
                                           Text="{Binding DiscountPercent, StringFormat=Скидка: {0}%}"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </DockPanel>
</Window>
